Для того, чтобы развернуть проект необходимо:
1. Вытянуть содержимое удаленного репозитория в локальный;
2. Поддтянуть все внешние зависимости composer`а с помощью команды: composer install;
3. Поддтянуть зависимости bower`a библиотеку Chartist.js с помощью команды: bower install chartist